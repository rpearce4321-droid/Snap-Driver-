# Change Context Log

Use this file to capture quick context for each change.
Section titles should be: "Area - YYYY-MM-DD".

## Feed Inline Actions - 2026-01-14
- Seeker feed cards now expand inline within the feed pane instead of using a modal pop-out.
- Feed responses and reactions remain attached to the expanded card, with direct message opening the compose popover.
- Messaging Center shows "View ad/route" chips that jump back to the related feed item.

## Routes (On-Demand) - 2026-01-14
- Add a route type switch: Dedicated vs Hotshot/On-Demand with different required fields.
- On-demand posts include countdown timer, pickup ETA window, and quick bid/accept actions for linked drivers.
- Responses/bids surface as feed items with priority ordering and alerting for retainers.
- Separate SLA and visibility rules (shorter expiry, higher feed priority) compared to dedicated routes.
